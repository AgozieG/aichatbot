╔═══════════════════════════════════════════════════════════════╗
║     🤖 YOUR PERSONALIZED SETUP GUIDE - READY TO GO! 🚀        ║
╚═══════════════════════════════════════════════════════════════╝

Your API key is already configured! Just follow these simple steps:


┌─────────────────────────────────────────────────────────────┐
│ STEP 1: INSTALL DEPENDENCIES (2 minutes)                     │
└─────────────────────────────────────────────────────────────┘

Open your terminal/command prompt in your project folder and run:

    npm install

This will download all required packages. Wait for it to complete...


┌─────────────────────────────────────────────────────────────┐
│ STEP 2: ORGANIZE YOUR FILES (1 minute)                       │
└─────────────────────────────────────────────────────────────┘

1. Make sure ALL these files are in your project folder:
   ✓ server.js
   ✓ package.json
   ✓ .env (this has your API key!)
   ✓ .gitignore
   ✓ index.html (your frontend)

2. Create a "public" folder:
   
   Mac/Linux:
       mkdir public
   
   Windows:
       mkdir public

3. Move your index.html into the public folder:
   
   Mac/Linux:
       mv index.html public/
   
   Windows:
       move index.html public\


┌─────────────────────────────────────────────────────────────┐
│ STEP 3: START THE SERVER (30 seconds)                        │
└─────────────────────────────────────────────────────────────┘

In your terminal, run:

    npm start

You should see this message:
    
    🤖 RoboCode AI Server is running!
    📡 Server: http://localhost:3001
    🔑 API Key configured: ✓ Yes
    
    Ready to help with robotics programming! 🚀


┌─────────────────────────────────────────────────────────────┐
│ STEP 4: OPEN YOUR WEBSITE (10 seconds)                       │
└─────────────────────────────────────────────────────────────┘

1. Open your web browser (Chrome, Firefox, Safari, Edge)

2. Go to this address:
   
   http://localhost:3001

3. You should see your RoboCode AI interface!


┌─────────────────────────────────────────────────────────────┐
│ STEP 5: TEST IT OUT! 🎉                                      │
└─────────────────────────────────────────────────────────────┘

Try asking these questions:

• "How do I make my robot turn 90 degrees using VEX blocks?"
• "Explain how to use the ultrasonic sensor in LEGO Mindstorms"
• "What's the difference between wait and wait until blocks?"
• "How can I make my robot follow a line?"


┌─────────────────────────────────────────────────────────────┐
│ YOUR PROJECT STRUCTURE (AFTER SETUP):                        │
└─────────────────────────────────────────────────────────────┘

your-project-folder/
├── server.js              ← Backend server
├── package.json           ← Dependencies
├── .env                   ← Your API key (already configured!)
├── .gitignore             ← Security
├── README.md              ← Full docs
├── node_modules/          ← (created after npm install)
└── public/                ← Create this folder!
    └── index.html         ← Move your HTML here!


┌─────────────────────────────────────────────────────────────┐
│ COMMON ISSUES & SOLUTIONS                                     │
└─────────────────────────────────────────────────────────────┘

❌ "Cannot find module"
   ✅ Run: npm install

❌ "EADDRINUSE: Port 3001 is already in use"
   ✅ Open .env file and change PORT=3001 to PORT=3002

❌ "Cannot GET /"
   ✅ Make sure index.html is in the public/ folder

❌ Page loads but chat doesn't work
   ✅ Check that server is running (npm start)
   ✅ Look for errors in the terminal

❌ "API Key not configured" 
   ✅ This shouldn't happen - your .env is ready!
   ✅ But if it does, make sure .env file is in the main folder


┌─────────────────────────────────────────────────────────────┐
│ IMPORTANT SECURITY NOTES 🔐                                  │
└─────────────────────────────────────────────────────────────┘

⚠️  Your API key is included in the .env file
⚠️  NEVER share the .env file with anyone
⚠️  NEVER upload .env to GitHub or any website
✅  The .gitignore file protects it automatically
✅  Treat your API key like a password


┌─────────────────────────────────────────────────────────────┐
│ API LIMITS (FREE TIER)                                        │
└─────────────────────────────────────────────────────────────┘

Your free Gemini API includes:
• 60 requests per minute
• 1,500 requests per day
• No credit card needed

This is plenty for testing and personal use!


┌─────────────────────────────────────────────────────────────┐
│ WHAT EACH FILE DOES                                           │
└─────────────────────────────────────────────────────────────┘

server.js          → Your backend (handles AI requests)
package.json       → Lists all dependencies
.env               → Your API key (secret!)
.gitignore         → Protects sensitive files
public/index.html  → Your beautiful frontend
node_modules/      → Downloaded packages


┌─────────────────────────────────────────────────────────────┐
│ MOBILE RESPONSIVENESS                                         │
└─────────────────────────────────────────────────────────────┘

✅ Already done! Your HTML has responsive design built in
✅ Works perfectly on phones, tablets, and desktops
✅ Test it by resizing your browser window


┌─────────────────────────────────────────────────────────────┐
│ STOPPING THE SERVER                                           │
└─────────────────────────────────────────────────────────────┘

When you're done testing:
Press: Ctrl + C (Windows/Linux) or Cmd + C (Mac)

To start again:
Run: npm start


┌─────────────────────────────────────────────────────────────┐
│ DEVELOPMENT TIPS                                              │
└─────────────────────────────────────────────────────────────┘

Auto-restart on changes:
    npm run dev

Check server status:
    Visit: http://localhost:3001/api/status

View server logs:
    Keep terminal window open while server runs


═══════════════════════════════════════════════════════════════
                 🎉 YOU'RE ALL SET! 🎉
                  
           Just run these 3 commands:
           
           1. npm install
           2. mkdir public && mv index.html public/
           3. npm start
           
           Then open: http://localhost:3001
═══════════════════════════════════════════════════════════════

Questions? Check the README.md file for detailed documentation!

Happy coding! 🤖✨
